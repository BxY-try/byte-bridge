name: byte_bridge
description: ByteBridge - Remote keyboard controller via WiFi
publish_to: 'none'
version: 1.0.5+9

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  socket_io_client: ^2.0.3+1
  cupertino_icons: ^1.0.6
  image_picker: ^1.1.2
  google_mlkit_text_recognition: ^0.13.0
  camera: ^0.11.0+2
  gal: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
